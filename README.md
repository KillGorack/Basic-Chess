# Basic Chess

A chess game built in Godot 4.

## Status

Work in progress.

## Setup

1. Open the project in Godot 4.7+.
2. Copy `_autoload/creds.gd.example` to `_autoload/creds.gd` and fill in your own API credentials (this file is gitignored and not tracked).
